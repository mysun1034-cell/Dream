# 설계·계약 문서 (Day 2 산출물) — 팀 [팀명]

> 2026-07-13 · 미니 프로젝트 2 -Spring AI를 활용한 기업서비스 구축 프로젝트
> 원본: design-contract-template.md · ES LMS

---

설계·계약 문서 (Day 2 산출물) — 팀 [팀명]

팀 레포에 docs/design.md(또는 README 설계 섹션)로 저장. ⟨ ⟩를 채운다. 프론트·백이 이 문서 하나로 병렬 개발한다 — 변수명·타입·성공/실패 코드가 100% 일치해야 한다. (채우는 법이 막히면 같은 폴더 design-contract-sample.md의 예시 참고.)

0. 메타
항목	내용
팀	⟨팀명⟩
작성일	2026-07-13
관련 Issue / PR	#⟨ ⟩
스택	Java 21 · Spring Boot 4.1.x · Spring AI 2.0.0 · Gemini(gemini-3.1-flash-lite) · React(Vite) · base 패키지 com.study
1. 문제·사용자 (한 문장)
사용자: ⟨누구인가 — 구체적으로⟩
문제: ⟨어떤 반복/불편을 겪는가⟩
한 문장 정의:

"우리 팀은 **⟨타깃 사용자⟩**를 위해 **⟨해결할 문제⟩**를 **⟨AI가 하는 핵심 일⟩**로 풀어 준다."

⚠ 넓으면(‘기업용 AI’) 실패. 누구의 어떤 반복 작업인지 좁힌다.

2. 기능 범위 — MoSCoW
구분	기능
Must (3~5, 절대 사수)	① ⟨ ⟩ ② ⟨ ⟩ ③ ⟨ ⟩ ④ ⟨ ⟩ ⑤ ⟨ ⟩
Should (시간 허락 시)	⟨ ⟩
Could (과감히 포기)	⟨ ⟩
오늘(Day2) 코드로 증명할 1개: ⟨Must 중 AI 호출 1개⟩
3. 핵심 시나리오 (MVP 한 흐름)
구분	흐름
입력 (User)	⟨무엇을 입력/클릭⟩
전송 (System)	React가 백엔드 API로 ⟨무엇⟩ 전송
처리 (System)	Spring AI가 ⟨무슨 처리⟩
저장 (System)	⟨무엇을 DB에 저장⟩
출력 (System)	⟨무엇이 화면에 표시⟩
4. 아키텍처 · 책임 분리 (Separation of Concerns)
React (Vite) ──HTTP──> Spring Boot (com.study) ──> Spring AI ──> Gemini
                            │
                            └──> DB (⟨테이블명⟩)

레이어	책임
React	사용자 입력 수신, API 요청, 로딩/오류 상태 관리
Controller	HTTP 요청 수신, 데이터 검증, Service로 라우팅
Service	프롬프트 구성, AI 호출, 응답 가공, 저장 지시
Spring AI & Repository	AI 모델 통신(ChatClient), DB CRUD(Repository)
5. API 계약 ⭐ (핵심)
5.1 엔드포인트 목록
Method	경로	설명
⟨POST⟩	/api/⟨ ⟩	⟨하는 일⟩
⟨ ⟩	/api/⟨ ⟩	⟨ ⟩
5.2 상세 — ⟨METHOD⟩ /api/⟨ ⟩

요청 (Request Body)

{ "⟨필드⟩": "⟨예시 값⟩" }

필드	타입	필수	설명
⟨ ⟩	⟨string⟩	⟨✅⟩	⟨ ⟩

응답 — 성공 200 OK

{
  "⟨필드⟩": "⟨값⟩"
}

필드	타입	설명
⟨ ⟩	⟨ ⟩	⟨ ⟩

응답 — 오류 (오류 형태도 계약)

상태 코드	상황	응답 예시
400 Bad Request	⟨입력 누락 등⟩	{ "message": "⟨안내 문구⟩" }
500 Internal Server Error	⟨AI 호출 실패 등⟩	{ "message": "⟨안내 문구⟩" } (DB 저장은 롤백)

프론트는 이 status·JSON 형태에 맞춰 로딩/에러 UI를 만든다 → 성공·실패 형태를 먼저 합의.

6. 데이터 모델 (DB 스키마)

5일 프로젝트 → 단일 테이블 권장. 복잡한 다대다는 피하고, 다시 조회할 필수 데이터만 저장.

⟨테이블명⟩

컬럼	타입	설명
id	Long (PK, auto)	식별자
⟨ ⟩	⟨ ⟩	⟨ ⟩
⟨ ⟩	⟨ ⟩	⟨ ⟩
createdAt	LocalDateTime	생성 시각
7. AI 연동 설계
7.1 프롬프트 해부학

[시스템 역할] + [작업 지시] + [출력 형식] + [사용자 입력] = 완성된 프롬프트

파트	내용
역할	"당신은 ⟨역할⟩입니다."
작업	"⟨무엇을 하라⟩."
출력 형식	"⟨필드1⟩, ⟨필드2⟩ 를 포함하는 JSON으로 반환하세요."
사용자 입력 (동적)	{⟨필드⟩} — React에서 받은 실제 값
7.2 구조화 출력 (평문 아님)
AI 반환 형식 (record): ⟨ResultName⟩(⟨String 필드…⟩)
.content()(평문) ❌ → .entity(⟨ResultName⟩.class) ✅
필드 매핑: AI 출력 필드 중 무엇을 DB/API로 보낼지 명시 (⟨ ⟩)
7.3 Spring AI 기능 매핑 (최소 2개)
우리 기능	쓰는 Spring AI 기능
⟨ ⟩	챗 (ChatClient)
⟨ ⟩	구조화 출력 (.entity)
⟨ ⟩	⟨멀티모달·툴/MCP·대화 메모리·스트리밍 중⟩
8. 예외 설계 (실패를 먼저 설계)
입력	시스템 반응
입력값 누락	400 + "⟨안내 문구⟩"
AI API 호출 실패	500 → DB 저장 취소(트랜잭션 롤백) 및 재시도 유도
API Key 누락	프론트 노출 금지, 백엔드 환경변수(${GOOGLE_API_KEY})에서만
9. 오늘의 완료 기준 (공식 Execution Checklist)
 1. 해결할 문제와 사용자가 한 문장으로 정의됨 (§1)
 2. Must 기능(3~5) + 핵심 시나리오 확정 (§2·§3)
 3. API 계약(URL·Method·JSON·오류) 문서화 (§5)
 4. (가장 중요) Spring AI 핵심 호출이 코드로 1회 성공 (§7 기반, curl로 확인)
# §5 계약대로 호출 확인
curl -X POST http://localhost:8080/api/⟨ ⟩ \
  -H "Content-Type: application/json" \
  -d '{"⟨필드⟩":"⟨값⟩"}'


각자 만든 코드가 아니라 연결되어 실행되는 하나의 서비스가 결과물이다.