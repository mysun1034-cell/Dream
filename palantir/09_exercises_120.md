# 연습문제 120개: 정답과 해설

정답을 보기 전에 자신의 예상 결과·가정·검증 방법을 먼저 적으세요. 설계형 문제에는 여러 타당한 답이 있을 수 있습니다. 아래 답은 판단 근거를 보여 주는 한 가지 기준입니다. 일부 답의 SQL/Python 조각은 설명을 위한 부분 코드이며, 본문 전체 예제와 달리 모든 조각을 독립 실행 검증한 것은 아닙니다.

## Python 기본: 코드가 낯선 단계에서 업무 파일을 다루는 단계로

<a id="py-b01-1"></a>
### PY-B01-1

**문제:** 지연 3건, 측정 가능한 방문 12건의 지연율을 소수점 한 자리로 출력하라.

**힌트:** 먼저 3/12를 계산하고 출력 형식을 적용한다.

**정답과 이유**

```python
late, eligible = 3, 12
print(f"{late / eligible:.1%}")  # 25.0%
```
비율은 0.25로 보관하고, 보고서에서만 25.0%로 표현한다.

[해당 강의로 돌아가기](#py-b01)

<a id="py-b01-2"></a>
### PY-B01-2

**문제:** `late = "6"`으로 바꾸면 왜 기존 나눗셈이 실패하는가?

**힌트:** 값의 모양이 아니라 자료형을 본다.

**정답과 이유**

"6"은 문자열이다. 문자열을 숫자로 나누는 연산은 정의되어 있지 않다. `int(late)`로 변환할 수 있으나 "six"처럼 변환 불가능한 입력은 별도 검증해야 한다.

[해당 강의로 돌아가기](#py-b01)

<a id="py-b02-1"></a>
### PY-B02-1

**문제:** " 12500 "을 정수 cents로 바꾸고 125.00 단위로 출력하라.

**힌트:** strip → int → Decimal 순서다.

**정답과 이유**

```python
from decimal import Decimal
cents = int(" 12500 ".strip())
print(f"{Decimal(cents) / Decimal('100'):.2f}")
```

[해당 강의로 돌아가기](#py-b02)

<a id="py-b02-2"></a>
### PY-B02-2

**문제:** "completed"를 정리할 수 있으니 "completeed"도 자동으로 완료 처리해야 하는가?

**힌트:** 정규화와 의미 추측을 구분한다.

**정답과 이유**

아니다. 공백·대소문자 정리는 명확하지만 오타의 뜻은 확정할 수 없다. 허용된 상태 목록에 없으면 격리하고 원본과 오류 사유를 기록한다. 자동 교정 정책이 필요하면 별도 승인을 받는다.

[해당 강의로 돌아가기](#py-b02)

<a id="py-b03-1"></a>
### PY-B03-1

**문제:** delay가 0, 10, 11, None일 때 완료 방문의 라벨을 각각 써라.

**힌트:** 조건을 위에서 아래로 손으로 실행한다.

**정답과 이유**

순서대로 on_time, on_time, late, unknown이다. 0은 실제 관측값이고 None은 관측값 부재다.

[해당 강의로 돌아가기](#py-b03)

<a id="py-b03-2"></a>
### PY-B03-2

**문제:** 취소 방문의 delay가 30이어도 지연율에 넣지 않도록 설명하라.

**힌트:** 규칙에서 분모에 들어가는 상태를 찾는다.

**정답과 이유**

첫 조건에서 status != "completed"가 참이므로 excluded다. 시간이 있더라도 취소 건을 완료 건의 지연율에 섞지 않는다. 취소율은 별도 지표로 만든다.

[해당 강의로 돌아가기](#py-b03)

<a id="py-b04-1"></a>
### PY-B04-1

**문제:** [1,2,3,4]에서 마지막 두 값을 꺼내라.

**힌트:** 음수 인덱스와 슬라이스를 사용한다.

**정답과 이유**

```python
values = [1, 2, 3, 4]
print(values[-2:])  # [3, 4]
```

[해당 강의로 돌아가기](#py-b04)

<a id="py-b04-2"></a>
### PY-B04-2

**문제:** `a=[{"status":"planned"}]; b=a.copy()` 후 b[0]의 상태를 바꾸면 a도 바뀌는가?

**힌트:** 바깥 리스트와 내부 딕셔너리를 나누어 생각한다.

**정답과 이유**

바뀐다. 얕은 복사는 내부 딕셔너리를 공유한다. 이 수준의 데이터는 `[row.copy() for row in a]`로 각 행을 복사할 수 있다. 더 깊은 중첩 구조는 별도의 복사 설계가 필요하다.

[해당 강의로 돌아가기](#py-b04)

<a id="py-b05-1"></a>
### PY-B05-1

**문제:** status를 키로, 건수를 값으로 하는 딕셔너리에 완료 18, 취소 3, 예정 3을 담아라.

**힌트:** 각 키는 문자열이고 값은 정수다.

**정답과 이유**

```python
counts = {"completed": 18, "cancelled": 3, "planned": 3}
print(sum(counts.values()))  # 24
```

[해당 강의로 돌아가기](#py-b05)

<a id="py-b05-2"></a>
### PY-B05-2

**문제:** 집합으로 중복을 제거하면 최신 수정 행이 남는가?

**힌트:** 집합에는 최신 시각을 비교하는 규칙이 없다.

**정답과 이유**

보장되지 않는다. 최신 행 선택에는 visit_id별로 updated_at을 비교하고, 시각이 같으면 source_sequence 같은 안정적인 동률 규칙이 필요하다. 집합은 존재 여부를 표현하는 도구이지 최신 상태 복원 알고리즘이 아니다.

[해당 강의로 돌아가기](#py-b05)

<a id="py-b06-1"></a>
### PY-B06-1

**문제:** 위 데이터의 평균 지연 분을 구하라. 음수는 없고 None만 제외한다.

**힌트:** 유효값의 합을 유효값 개수로 나눈다.

**정답과 이유**

```python
values = [0, 15, 30, 5, None, 10]
known = [v for v in values if v is not None]
mean_delay = sum(known) / len(known) if known else None
print(mean_delay)  # 12.0
```

[해당 강의로 돌아가기](#py-b06)

<a id="py-b06-2"></a>
### PY-B06-2

**문제:** `range(1,4)`가 만드는 값과 방문 ID에 사용할 때 주의점을 설명하라.

**힌트:** 끝 값은 포함되지 않는다.

**정답과 이유**

1,2,3이다. 실제 방문 ID가 연속이라고 가정하면 삭제·누락된 ID를 놓친다. 실제 데이터에서는 범위를 새로 만드는 대신 읽어온 행의 ID를 순회한다.

[해당 강의로 돌아가기](#py-b06)

<a id="py-b07-1"></a>
### PY-B07-1

**문제:** 분자 4, 분모 10을 넣어 0.4를 얻고, 분자 11·분모 10은 오류가 나도록 확인하라.

**힌트:** 정상 경로와 실패 경로를 둘 다 실행한다.

**정답과 이유**

`assert rate(4, 10) == 0.4`로 정상 값을 검사한다. `rate(11,10)`은 ValueError를 발생시킨다. 화면 확인만으로 끝내지 말고 PY-I09에서 예외 발생 자체를 테스트로 고정한다.

[해당 강의로 돌아가기](#py-b07)

<a id="py-b07-2"></a>
### PY-B07-2

**문제:** 타입 힌트가 있으니 `rate("6","15")`도 안전하게 정수로 변환되는가?

**힌트:** 힌트와 런타임 변환은 다르다.

**정답과 이유**

아니다. 타입 힌트는 자동 변환이 아니다. 외부 입력 경계에서 `int` 변환과 오류 처리를 수행하거나 명시적 데이터 검증 도구를 사용한다.

[해당 강의로 돌아가기](#py-b07)

<a id="py-b08-1"></a>
### PY-B08-1

**문제:** 방문 CSV에서 status가 cancelled인 건수를 구하라.

**힌트:** CSV의 각 행은 딕셔너리다.

**정답과 이유**

```python
from course_lib import read_csv
rows = read_csv("visits")
print(sum(row["status"] == "cancelled" for row in rows))  # 3
```

[해당 강의로 돌아가기](#py-b08)

<a id="py-b08-2"></a>
### PY-B08-2

**문제:** CSV의 빈 actual_start가 Python에서 항상 None으로 읽히는가?

**힌트:** csv.DictReader가 반환하는 값의 종류를 확인한다.

**정답과 이유**

이 실습의 표준 csv.DictReader에서는 빈 문자열 ""로 읽힌다. SQLite에서는 NULL, pandas에서는 결측 표현으로 읽힐 수 있다. 각 입력 경계에서 표현을 통일해야 한다.

[해당 강의로 돌아가기](#py-b08)

<a id="py-b09-1"></a>
### PY-B09-1

**문제:** 빈 문자열과 "0"을 입력에 추가하면 어떻게 처리되는가?

**힌트:** 형 변환 실패와 범위 검증 실패를 구분한다.

**정답과 이유**

빈 문자열은 int 변환에서 ValueError, "0"은 변환에 성공한 뒤 양수 조건에서 ValueError가 난다. 둘 다 격리되지만 사유는 다르다.

[해당 강의로 돌아가기](#py-b09)

<a id="py-b09-2"></a>
### PY-B09-2

**문제:** 코드 안의 `valid.apend(...)` 오타도 ValueError로 잡히는가?

**힌트:** 잘못된 메서드 이름이 만드는 오류 종류를 생각한다.

**정답과 이유**

AttributeError가 발생하며 현재 except ValueError에는 잡히지 않는다. 이는 의도된 결과다. 프로그램 버그를 수정해야 하며 잘못된 데이터 한 행으로 취급하면 안 된다.

[해당 강의로 돌아가기](#py-b09)

<a id="py-b10-1"></a>
### PY-B10-1

**문제:** 결과에 분석 기간 2026-08-01~2026-08-03을 메타데이터로 넣어라.

**힌트:** 계산 결과와 실행 맥락을 분리해 중첩 딕셔너리를 만든다.

**정답과 이유**

```python
from course_lib import read_csv, kpi
report = {
    "period": {"start": "2026-08-01", "end_exclusive": "2026-08-04"},
    "metric_version": "late-v1",
    "metrics": kpi(read_csv("visits")),
}
print(report["metrics"]["late_rate"])
```
기간 필드는 설명일 뿐 자동 필터가 아니다. 입력이 바뀌면 실제 날짜 필터도 함께 구현해야 한다.

[해당 강의로 돌아가기](#py-b10)

<a id="py-b10-2"></a>
### PY-B10-2

**문제:** PY-B 과정의 졸업 설명을 3문장으로 작성하라.

**힌트:** 입력·규칙·결과와 한계를 포함한다.

**정답과 이유**

예시: 합성 방문 CSV 24건을 읽었다. 완료이면서 실제 시작 시간이 있는 15건 중 10분 초과 지연은 6건으로 40%다. 완료했지만 시간이 없는 3건은 지연 여부를 알 수 없으므로 별도 품질 지표로 보고한다.

[해당 강의로 돌아가기](#py-b10)

## SQL 기본: 표를 읽는 단계에서 정확한 업무 질문을 만드는 단계로

<a id="sql-b01-1"></a>
### SQL-B01-1

**문제:** 고객 테이블에서 client_id와 branch_id를 ID순으로 첫 네 행 조회하라.

**힌트:** 표 이름은 clients다.

**정답과 이유**

```sql
SELECT client_id, branch_id
FROM clients
ORDER BY client_id
LIMIT 4;
```
결과는 (1,1), (2,1), (3,1), (4,2)다.

[해당 강의로 돌아가기](#sql-b01)

<a id="sql-b01-2"></a>
### SQL-B01-2

**문제:** SELECT 결과에서 client_id=1이 여러 번 나오는 것은 무조건 중복 오류인가?

**힌트:** 방문과 고객의 grain을 비교한다.

**정답과 이유**

아니다. 같은 고객의 여러 방문일 수 있다. visit_id가 고유하고 실제 서로 다른 방문이면 정상이다. 중복 여부는 행의 업무 단위와 키 기준으로 판단한다.

[해당 강의로 돌아가기](#sql-b01)

<a id="sql-b02-1"></a>
### SQL-B02-1

**문제:** branch_id=3인 방문 중 planned가 아닌 방문 ID를 조회하라.

**힌트:** 같지 않다는 SQL 연산자는 <>다.

**정답과 이유**

```sql
SELECT visit_id FROM visits
WHERE branch_id = 3 AND status <> 'planned'
ORDER BY visit_id;
```
결과 ID는 8,16,24다.

[해당 강의로 돌아가기](#sql-b02)

<a id="sql-b02-2"></a>
### SQL-B02-2

**문제:** `fee_cents BETWEEN 10000 AND 12000`은 12000도 포함하는가?

**힌트:** BETWEEN의 양쪽 경계를 확인한다.

**정답과 이유**

포함한다. BETWEEN은 양끝을 포함한다. 숫자 범위에는 적절할 수 있지만 연속된 시간 창을 겹치지 않게 나눌 때는 >= 시작 AND < 끝을 사용한다.

[해당 강의로 돌아가기](#sql-b02)

<a id="sql-b03-1"></a>
### SQL-B03-1

**문제:** 완료 방문 중 actual_start가 없는 방문 ID를 조회하라.

**힌트:** status 조건과 IS NULL을 함께 쓴다.

**정답과 이유**

```sql
SELECT visit_id FROM visits
WHERE status = 'completed' AND actual_start IS NULL
ORDER BY visit_id;
```
결과는 6,14,22다.

[해당 강의로 돌아가기](#sql-b03)

<a id="sql-b03-2"></a>
### SQL-B03-2

**문제:** NULL을 모두 0분으로 바꿔 지연율을 계산하면 왜 낮아질 수 있는가?

**힌트:** 분자는 그대로인데 분모에 누가 추가되는지 본다.

**정답과 이유**

알 수 없는 완료 방문을 정시 방문처럼 분모에 넣기 때문이다. 실제 6/15=40% 대신 6/18≈33.3%가 되어 미측정을 개선처럼 오해할 수 있다.

[해당 강의로 돌아가기](#sql-b03)

<a id="sql-b04-1"></a>
### SQL-B04-1

**문제:** 방문한 고객의 고유 ID를 오름차순으로 조회하라.

**힌트:** DISTINCT는 client_id 하나에 적용한다.

**정답과 이유**

```sql
SELECT DISTINCT client_id FROM visits ORDER BY client_id;
```
1부터 8까지이며 전체 등록 고객 12명과 다르다.

[해당 강의로 돌아가기](#sql-b04)

<a id="sql-b04-2"></a>
### SQL-B04-2

**문제:** 비용 상위 5건을 조회하는데 동일 비용 행의 순서가 바뀌어도 괜찮은가?

**힌트:** 재현 가능한 보고서와 페이지 이동을 생각한다.

**정답과 이유**

사람이 보는 단순 목록이라도 재현성과 테스트를 위해 동률 기준이 필요하다. 페이지 단위 조회에서는 순서 변화로 중복·누락이 생길 수 있으므로 고유 키를 마지막 정렬 기준에 넣는다.

[해당 강의로 돌아가기](#sql-b04)

<a id="sql-b05-1"></a>
### SQL-B05-1

**문제:** 상태별 방문 수를 구하라.

**힌트:** status를 GROUP BY에 넣는다.

**정답과 이유**

```sql
SELECT status, COUNT(*) AS visits
FROM visits
GROUP BY status
ORDER BY status;
```
cancelled=3, completed=18, planned=3이다.

[해당 강의로 돌아가기](#sql-b05)

<a id="sql-b05-2"></a>
### SQL-B05-2

**문제:** 지점 A가 1/2, 지점 B가 9/18 지연이면 전체 지연율은 얼마인가?

**힌트:** 분자끼리·분모끼리 합친다.

**정답과 이유**

(1+9)/(2+18)=50%다. 이 경우 단순 평균도 우연히 같지만 일반적으로 그렇지 않다. A가 1/2, B가 0/18이면 전체는 5%이고 지점 비율 단순 평균은 25%다.

[해당 강의로 돌아가기](#sql-b05)

<a id="sql-b06-1"></a>
### SQL-B06-1

**문제:** 전체 취소율을 소수 네 자리까지 계산하라.

**힌트:** 취소 건수를 전체 24건으로 나눈다.

**정답과 이유**

```sql
SELECT ROUND(1.0 * SUM(CASE WHEN status='cancelled' THEN 1 ELSE 0 END)
       / NULLIF(COUNT(*),0),4) AS cancellation_rate
FROM visits;
```
결과는 0.125이며 12.5%다.

[해당 강의로 돌아가기](#sql-b06)

<a id="sql-b06-2"></a>
### SQL-B06-2

**문제:** 전체 완료율이 75%인 이유만으로 운영이 나쁘다고 결론 내릴 수 있는가?

**힌트:** 예정·취소 상태와 관측 시점을 확인한다.

**정답과 이유**

아니다. 3건은 예정 상태이고 3건은 취소다. 운영 기준 시점, 미래 방문 포함 여부, 취소 사유, 목표값을 확인해야 한다. 구성비는 사실이지만 성과 판단에는 맥락이 더 필요하다.

[해당 강의로 돌아가기](#sql-b06)

<a id="sql-b07-1"></a>
### SQL-B07-1

**문제:** 방문 ID와 담당 직원 이름을 연결하라. 담당자가 없는 방문은 이번에는 제외한다.

**힌트:** workers.worker_id와 visits.worker_id를 연결한다.

**정답과 이유**

```sql
SELECT v.visit_id, w.worker_name
FROM visits v
JOIN workers w ON v.worker_id = w.worker_id
ORDER BY v.visit_id;
```
예정 3건은 worker_id가 NULL이라 연결되지 않아 21행이다.

[해당 강의로 돌아가기](#sql-b07)

<a id="sql-b07-2"></a>
### SQL-B07-2

**문제:** 오른쪽 지점 표에서 branch_id=1이 두 행이면 어떤 일이 생기는가?

**힌트:** 1번 지점 방문 한 행에 연결되는 상대가 몇 개인지 센다.

**정답과 이유**

1번 지점의 각 방문이 두 번 나타난다. 해당 방문 수·비용을 집계하면 부풀려진다. DISTINCT로 숨기기보다 키 고유성 위반을 먼저 수정한다.

[해당 강의로 돌아가기](#sql-b07)

<a id="sql-b08-1"></a>
### SQL-B08-1

**문제:** 모든 지점의 예정 방문 수를 구하라.

**힌트:** ON의 상태를 planned로 바꾼다.

**정답과 이유**

```sql
SELECT b.branch_id, COUNT(v.visit_id) AS planned_visits
FROM branches b
LEFT JOIN visits v ON b.branch_id=v.branch_id AND v.status='planned'
GROUP BY b.branch_id ORDER BY b.branch_id;
```
결과는 지점별 0,0,3,0이다.

[해당 강의로 돌아가기](#sql-b08)

<a id="sql-b08-2"></a>
### SQL-B08-2

**문제:** 이 쿼리에서 COUNT(*)와 COUNT(v.visit_id)는 4번 지점에서 각각 얼마인가?

**힌트:** 보존된 왼쪽 행과 오른쪽 값의 유무를 구분한다.

**정답과 이유**

COUNT(*)는 1, COUNT(v.visit_id)는 0이다. 완료 방문이 없는 지점에 대해 오른쪽 열이 NULL인 행 하나가 생성되기 때문이다.

[해당 강의로 돌아가기](#sql-b08)

<a id="sql-b09-1"></a>
### SQL-B09-1

**문제:** fee_demo에 (2,-1,"planned")를 넣으면 어떻게 되어야 하는가?

**힌트:** 음수 금액 규칙을 찾는다.

**정답과 이유**

CHECK 위반으로 거부되어야 한다. 잘못된 데이터를 0으로 자동 변경하는 것은 별도 업무 결정이며 제약조건 실패와 구별한다.

[해당 강의로 돌아가기](#sql-b09)

<a id="sql-b09-2"></a>
### SQL-B09-2

**문제:** 고객 이름을 기본키로 쓰지 않는 이유를 세 가지 설명하라.

**힌트:** 중복·변경·입력 차이를 생각한다.

**정답과 이유**

동명이인이 존재할 수 있고, 이름이 바뀔 수 있으며, 띄어쓰기·언어·표기 차이로 같은 사람을 다르게 기록할 수 있다. 안정적인 고객 ID와 식별·통합 절차를 사용한다.

[해당 강의로 돌아가기](#sql-b09)

<a id="sql-b10-1"></a>
### SQL-B10-1

**문제:** 예정 상태인 visit_id=1만 삭제하는 쿼리를 작성하되 실제 데이터에서는 실행하지 마라.

**힌트:** ID와 기존 상태를 함께 제한한다.

**정답과 이유**

```sql
-- 폐기 가능한 change_demo에서만 실행
DELETE FROM change_demo
WHERE visit_id=1 AND status='planned';
```
사전 SELECT와 예상 영향 행 수 1을 확인한다. 이미 다른 상태면 0행 변경이므로 성공 메시지를 내기 전에 확인해야 한다.

[해당 강의로 돌아가기](#sql-b10)

<a id="sql-b10-2"></a>
### SQL-B10-2

**문제:** 트랜잭션을 쓰면 WHERE 없이 전체 행을 바꿔도 안전한가?

**힌트:** 원자성과 업무 정확성은 다르다.

**정답과 이유**

아니다. 잘못된 전체 변경도 COMMIT하면 확정된다. ROLLBACK은 확정 전 되돌리기 수단이지 올바른 조건을 대신하지 않는다. 운영에서는 권한 제한·검토·백업·복구 절차가 추가로 필요하다.

[해당 강의로 돌아가기](#sql-b10)

## Python 중급: 데이터 분석을 재현 가능한 프로그램으로

<a id="py-i01-1"></a>
### PY-I01-1

**문제:** source와 rule_version 외의 메타데이터 키를 거부하도록 검사 코드를 작성하라.

**힌트:** 집합 차집합으로 알 수 없는 키를 찾는다.

**정답과 이유**

```python
allowed = {"source", "rule_version"}
unknown = set(metadata) - allowed
if unknown:
    raise ValueError(f"unknown metadata: {sorted(unknown)}")
```
함수 본문의 계산 전에 넣는다. 이렇게 하면 treshold 같은 오타를 조용히 받아들이지 않는다.

[해당 강의로 돌아가기](#py-i01)

<a id="py-i01-2"></a>
### PY-I01-2

**문제:** *args와 **kwargs의 차이를 함수 호출 예로 설명하라.

**힌트:** 위치와 이름이 있는 입력을 비교한다.

**정답과 이유**

`f(1,2,source="csv")`에서 `def f(*args,**kwargs)`라면 args는 (1,2), kwargs는 {"source":"csv"}다. 호출 쪽의 `f(**settings)`는 딕셔너리를 키워드 인자로 펼치는 반대 방향의 사용이다.

[해당 강의로 돌아가기](#py-i01)

<a id="py-i02-1"></a>
### PY-I02-1

**문제:** Visit(102,"planned",None)의 측정 가능 여부를 예측하라.

**힌트:** 상태와 값 존재를 모두 검사한다.

**정답과 이유**

False다. 완료가 아니므로 실제 시간이 기록되어 있더라도 이 KPI의 측정 대상이 아니다.

[해당 강의로 돌아가기](#py-i02)

<a id="py-i02-2"></a>
### PY-I02-2

**문제:** Visit 클래스 안에 DB 비밀번호·HTTP 요청·보고서 출력까지 모두 넣으면 어떤 문제가 생기는가?

**힌트:** 변경 이유와 테스트 비용을 생각한다.

**정답과 이유**

한 클래스가 여러 이유로 바뀌고 DB 없이 계산을 테스트하기 어려워진다. 도메인 규칙, 저장소, API, 출력기를 나누고 필요한 인터페이스로 연결한다.

[해당 강의로 돌아가기](#py-i02)

<a id="py-i03-1"></a>
### PY-I03-1

**문제:** 1~24까지 방문 ID의 합을 제너레이터로 계산하라.

**힌트:** 새 이터레이터를 만들어야 한다.

**정답과 이유**

```python
from pathlib import Path
print(sum(iter_visit_ids(Path("data/visits.csv"))))  # 300
```

[해당 강의로 돌아가기](#py-i03)

<a id="py-i03-2"></a>
### PY-I03-2

**문제:** 고유 고객 ID가 1천만 개이면 set을 사용하는 중복 제거의 메모리는 일정한가?

**힌트:** 보관하는 고유값 수를 센다.

**정답과 이유**

일정하지 않다. O(u), u는 고유 고객 수다. DB의 고유 인덱스, 외부 정렬, 디스크 기반 처리 등으로 메모리 밖에서 관리할 전략이 필요하다.

[해당 강의로 돌아가기](#py-i03)

<a id="py-i04-1"></a>
### PY-I04-1

**문제:** 취소 방문의 지점별 건수를 pandas로 계산하라.

**힌트:** 상태 필터를 바꾸고 size를 사용한다.

**정답과 이유**

```python
cancelled = visits.loc[visits["status"].eq("cancelled")]
print(cancelled.groupby("branch_id").size().to_dict())  # {1: 3}
```
다른 지점도 0으로 표시하려면 지점 목록에 다시 연결해야 한다.

[해당 강의로 돌아가기](#py-i04)

<a id="py-i04-2"></a>
### PY-I04-2

**문제:** CSV의 visit_id가 숫자처럼 보여도 고객의 외부 ID라면 정수로 바꿔도 되는가?

**힌트:** 선행 0과 계산 필요성을 생각한다.

**정답과 이유**

자동으로 바꾸면 안 된다. "0012"와 "12"를 구분해야 할 수 있고 산술 연산도 필요 없다. 외부 식별자는 dtype="string" 등으로 읽는 입력 계약을 정한다.

[해당 강의로 돌아가기](#py-i04)

<a id="py-i05-1"></a>
### PY-I05-1

**문제:** branches에 첫 행을 복제해 붙이고 merge를 다시 실행하면 어떤 결과를 기대해야 하는가?

**힌트:** many_to_one 계약을 일부러 깨 본다.

**정답과 이유**

오른쪽 키의 고유성이 깨졌으므로 pandas MergeError가 나야 한다. 오류 없이 방문 수가 늘어나는 것을 정상으로 받아들이면 안 된다.

[해당 강의로 돌아가기](#py-i05)

<a id="py-i05-2"></a>
### PY-I05-2

**문제:** 두 표의 branch_id가 모두 결측일 때 SQL과 pandas 결과가 달라질 수 있는 이유는?

**힌트:** NULL 매칭 규칙을 비교한다.

**정답과 이유**

일반 SQL에서 NULL=NULL은 참이 아니어서 equality join에 매칭되지 않는다. pandas merge는 결측 키끼리 매칭할 수 있다. 결측 키를 먼저 처리하고 결과를 교차 검증한다.

[해당 강의로 돌아가기](#py-i05)

<a id="py-i06-1"></a>
### PY-I06-1

**문제:** 2026-08-31 16:00 UTC는 서울 기준 어느 날짜인가?

**힌트:** 9시간을 더하되 월이 바뀌는지 본다.

**정답과 이유**

2026-09-01 01:00 +09:00이다. UTC 8월 집계에는 들어가지만 서울 현지 8월 집계에는 들어가지 않는다.

[해당 강의로 돌아가기](#py-i06)

<a id="py-i06-2"></a>
### PY-I06-2

**문제:** naive datetime 두 개를 빼면 오류 없이 숫자가 나오는데 왜 위험할 수 있는가?

**힌트:** 숫자가 나오는 것과 시간대 계약이 맞는 것은 다르다.

**정답과 이유**

서로 다른 지역의 현지 시각을 같은 기준으로 오해할 수 있다. 입력에서 시간대가 누락되면 거부하거나 명확한 원천 시스템 계약에 따라 해석해야 한다.

[해당 강의로 돌아가기](#py-i06)

<a id="py-i07-1"></a>
### PY-I07-1

**문제:** branch_id=2의 완료 방문 수를 파라미터로 조회하라.

**힌트:** 값 두 개에 자리표시자 두 개를 사용한다.

**정답과 이유**

```python
from course_lib import query
rows = query("SELECT COUNT(*) AS n FROM visits WHERE branch_id=? AND status=?",
             (2, "completed"))
print(rows[0]["n"])  # 9
```

[해당 강의로 돌아가기](#py-i07)

<a id="py-i07-2"></a>
### PY-I07-2

**문제:** psycopg로 전환할 때 SELECT의 값 자리는 어떻게 쓰는가?

**힌트:** 자리표시자 문법이 드라이버마다 다르다.

**정답과 이유**

`conn.execute("SELECT ... WHERE branch_id=%s", (2,))`처럼 사용한다. %s에 따옴표를 붙이거나 문자열 % 연산으로 직접 치환하지 않는다. 연결 주소·자격증명은 실행 환경에서 전달한다.

[해당 강의로 돌아가기](#py-i07)

<a id="py-i08-1"></a>
### PY-I08-1

**문제:** 두 번째 응답도 next_cursor="page2"를 반환하면 어떻게 되는가?

**힌트:** seen 집합의 목적을 확인한다.

**정답과 이유**

반복 커서 오류가 발생해야 한다. 무한 루프를 막는다. items를 이미 모았더라도 오류를 성공 결과로 숨기지 않고 부분 결과 여부를 명확히 처리한다.

[해당 강의로 돌아가기](#py-i08)

<a id="py-i08-2"></a>
### PY-I08-2

**문제:** 429 응답과 400 응답을 모두 즉시 10번 재시도해야 하는가?

**힌트:** 일시적인 제한과 잘못된 요청을 구분한다.

**정답과 이유**

아니다. 429는 서버의 재시도 안내와 전체 시간 예산을 따르고, 400은 보통 요청을 수정해야 한다. 재시도 간격과 최대 횟수를 제한하고 멱등성이 보장되는 작업인지 확인한다.

[해당 강의로 돌아가기](#py-i08)

<a id="py-i09-1"></a>
### PY-I09-1

**문제:** cancelled 상태가 delay=30이어도 excluded인지 테스트를 추가하라.

**힌트:** 새 테스트 함수는 test_로 시작한다.

**정답과 이유**

```python
def test_cancelled_is_excluded_even_with_timestamp():
    assert classify_visit("cancelled", 30) == "excluded"
```

[해당 강의로 돌아가기](#py-i09)

<a id="py-i09-2"></a>
### PY-I09-2

**문제:** 개발자가 지연 기준을 >10에서 >=10으로 바꾸면 어느 테스트가 잡는가?

**힌트:** 경계값 10의 기대값을 찾는다.

**정답과 이유**

(10,"on_time") 케이스가 실패한다. 이 실패는 단순 코드 문제일 수도 있고 승인된 업무 규칙 변경일 수도 있다. 규칙 변경이라면 버전·과거 재계산 정책·테스트를 함께 변경한다.

[해당 강의로 돌아가기](#py-i09)

<a id="py-i10-1"></a>
### PY-I10-1

**문제:** 결측 완료 방문이 3건이 아니라 0건이면 가능한 범위는 어떻게 되는가?

**힌트:** 분모도 completed=15인 일관된 사례를 생각한다.

**정답과 이유**

관측 완료가 15건이고 모두 측정되었다면 하한·상한 모두 6/15=40%다. 다른 값은 그대로 두고 unknown만 0으로 바꾸는 식으로 모순된 숫자를 만들지 않는다.

[해당 강의로 돌아가기](#py-i10)

<a id="py-i10-2"></a>
### PY-I10-2

**문제:** “지점 B가 더 나쁘니 직원을 늘리자”에 앞서 필요한 확인을 세 가지 써라.

**힌트:** 비교 집단·누락·행동 가능성을 확인한다.

**정답과 이유**

업무 난이도와 시간대 등 사례 구성이 비슷한지, 지점별 측정 누락률이 다른지, 지연 원인이 인력 부족인지 배정·이동·기록 문제인지 확인한다. 그 뒤 제한된 파일럿과 성공·중단 기준을 설계한다.

[해당 강의로 돌아가기](#py-i10)

## SQL 중급: 복잡한 데이터에서 믿을 수 있는 지표 만들기

<a id="sql-i01-1"></a>
### SQL-I01-1

**문제:** 완료 방문 비용 합계가 60000 cents 이상인 지점을 조회하도록 바꿔라.

**힌트:** 최종 조건을 비용 합계에 적용한다.

**정답과 이유**

최종 WHERE를 `fee_cents >= 60000`으로 바꾸면 1번 63000, 2번 126000이 나온다. 값의 단위와 “완료 방문 요금 합계”라는 정의를 유지한다.

[해당 강의로 돌아가기](#sql-i01)

<a id="sql-i01-2"></a>
### SQL-I01-2

**문제:** CTE가 항상 성능을 높인다는 주장에 답하라.

**힌트:** 논리 구조와 실행 계획을 분리한다.

**정답과 이유**

아니다. CTE는 표현 구조이고 실제 최적화는 엔진·버전·쿼리 성질에 따라 달라진다. 물질화로 반복 계산을 줄일 수도 있고 조건 전달이 제한될 수도 있다. 실행 계획과 실제 측정으로 비교한다.

[해당 강의로 돌아가기](#sql-i01)

<a id="sql-i02-1"></a>
### SQL-I02-1

**문제:** 상태와 무관하게 방문 기록이 전혀 없는 고객을 구하라.

**힌트:** 하위 쿼리에서 상태 조건만 제거한다.

**정답과 이유**

```sql
SELECT c.client_id FROM clients c
WHERE NOT EXISTS (
  SELECT 1 FROM visits v WHERE v.client_id=c.client_id
)
ORDER BY c.client_id;
```
결과는 9,10,11,12다.

[해당 강의로 돌아가기](#sql-i02)

<a id="sql-i02-2"></a>
### SQL-I02-2

**문제:** EXISTS 대신 JOIN을 쓰면 어떤 추가 처리가 필요할 수 있는가?

**힌트:** 고객 한 명이 여러 방문과 연결되는 경우를 생각한다.

**정답과 이유**

JOIN 결과가 방문 수만큼 늘어나므로 고유 고객이 필요하면 DISTINCT나 그룹화가 필요하다. EXISTS는 존재 여부만 반영하므로 고객 grain을 자연스럽게 유지한다.

[해당 강의로 돌아가기](#sql-i02)

<a id="sql-i03-1"></a>
### SQL-I03-1

**문제:** 청구서를 입금에 바로 LEFT JOIN하고 청구액을 합치면 얼마가 나오는가?

**힌트:** 1번과 6번 청구액이 추가로 한 번씩 반복된다.

**정답과 이유**

135000 cents다. 원래 100000에 1번 10000과 6번 25000이 추가된다. 정상 입금 7행과 미입금 청구서 1행으로 연결 결과는 8행이다.

[해당 강의로 돌아가기](#sql-i03)

<a id="sql-i03-2"></a>
### SQL-I03-2

**문제:** 미입금 청구서만 찾는 완성 쿼리를 작성하라.

**힌트:** 입금 행이 전혀 없는지를 물어본다.

**정답과 이유**

```sql
SELECT i.invoice_id, i.amount_cents
FROM invoices i
WHERE NOT EXISTS (
  SELECT 1 FROM payments p WHERE p.invoice_id=i.invoice_id
)
ORDER BY i.invoice_id;
```
5번, 18000 cents다. 부분 입금으로 잔액이 남은 청구서와는 다른 집합이다.

[해당 강의로 돌아가기](#sql-i03)

<a id="sql-i04-1"></a>
### SQL-I04-1

**문제:** 고객별 최신 완료 방문을 조회하라.

**힌트:** ranked CTE의 FROM 다음에 완료 WHERE를 넣는다.

**정답과 이유**

`FROM visits WHERE status='completed'`로 랭킹 대상을 제한한다. 결과 고객은 1,2,4,5,6,8이며 최신 방문 ID는 17,18,20,21,22,24다.

[해당 강의로 돌아가기](#sql-i04)

<a id="sql-i04-2"></a>
### SQL-I04-2

**문제:** 동점 포함 상위 2개 비용 등급을 고르려면 ROW_NUMBER와 DENSE_RANK 중 무엇을 쓰는가?

**힌트:** 정확히 두 행인지 두 등급인지 확인한다.

**정답과 이유**

DENSE_RANK를 비용 내림차순으로 계산해 <=2를 고른다. 동점 포함이므로 결과 행 수는 두 행보다 많을 수 있다. ROW_NUMBER<=2는 동률 기준으로 정확히 두 행만 고른다.

[해당 강의로 돌아가기](#sql-i04)

<a id="sql-i05-1"></a>
### SQL-I05-1

**문제:** 각 청구액과 이전 청구액의 차이를 계산하라.

**힌트:** 현재 값에서 LAG를 뺀다.

**정답과 이유**

```sql
SELECT invoice_id,
       amount_cents - LAG(amount_cents) OVER (ORDER BY issued_on, invoice_id) AS delta
FROM invoices ORDER BY issued_on, invoice_id;
```
차이는 NULL,10000,-5000,-3000,6000,7000이다.

[해당 강의로 돌아가기](#sql-i05)

<a id="sql-i05-2"></a>
### SQL-I05-2

**문제:** 8월 1일과 8월 3일만 있는 표에서 LAG가 8월 3일 행에 반환하는 값은 어제 값인가?

**힌트:** 행의 인접성과 날짜의 인접성을 구분한다.

**정답과 이유**

아니다. 직전 관측 행인 8월 1일 값이다. “어제”가 필요하면 날짜 달력과 연결하거나 정확히 하루 차이인 행을 찾아야 한다.

[해당 강의로 돌아가기](#sql-i05)

<a id="sql-i06-1"></a>
### SQL-I06-1

**문제:** 서울 8월 전체를 겹치지 않는 시각 범위로 표현하라.

**힌트:** 시작 포함, 9월 시작 제외다.

**정답과 이유**

`scheduled_start >= TIMESTAMPTZ '2026-08-01 00:00:00+09' AND scheduled_start < TIMESTAMPTZ '2026-09-01 00:00:00+09'`다. UTC 월 경계와 다름을 설명해야 한다.

[해당 강의로 돌아가기](#sql-i06)

<a id="sql-i06-2"></a>
### SQL-I06-2

**문제:** 8월 4일의 집계 결과가 0이면 실제 방문이 없었다고 단정할 수 있는가?

**힌트:** 데이터 수집 성공 기록이 있는지 묻는다.

**정답과 이유**

단정할 수 없다. 수집 실패나 지연 적재일 수 있다. 해당 날짜·원천 시스템의 완료 플래그, 마지막 수신 시각, 예상 건수와 대사한 뒤 0을 확정한다.

[해당 강의로 돌아가기](#sql-i06)

<a id="sql-i07-1"></a>
### SQL-I07-1

**문제:** visits에서 방문 키 중복을 찾는 품질 쿼리를 작성하라.

**힌트:** 키별 COUNT가 1을 넘는지 검사한다.

**정답과 이유**

```sql
SELECT visit_id, COUNT(*) AS copies
FROM visits
GROUP BY visit_id
HAVING COUNT(*) > 1;
```
제공된 자료에서는 0행이 정상이다. 수집 staging에는 같은 키 중복이 있을 수 있으므로 별도 검사한다.

[해당 강의로 돌아가기](#sql-i07)

<a id="sql-i07-2"></a>
### SQL-I07-2

**문제:** 중복 판단을 SELECT DISTINCT *로 끝내면 무엇을 놓치는가?

**힌트:** 같은 방문의 서로 다른 상태 행을 생각한다.

**정답과 이유**

열 하나라도 다른 수정본은 남는다. 반대로 완전히 같은 값의 실제 서로 다른 사건을 ID 없이 저장했다면 구분하지 못한다. 업무 키·이벤트 키·버전이 필요하다.

[해당 강의로 돌아가기](#sql-i07)

<a id="sql-i08-1"></a>
### SQL-I08-1

**문제:** 가입일을 무시한 전체 8월 완료 활동 고객 수와 비율은?

**힌트:** 두 코호트의 분자와 분모를 합친다.

**정답과 이유**

활성 고객 6명, 등록 고객 12명, 활동률 50%다. 완료 방문은 18건이므로 방문 건수를 분자에 넣으면 전혀 다른 지표가 된다.

[해당 강의로 돌아가기](#sql-i08)

<a id="sql-i08-2"></a>
### SQL-I08-2

**문제:** 9월 30일에 9월 가입자의 가입 후 30일 리텐션을 모두 계산해도 되는가?

**힌트:** 가입 후 30일이 아직 지나지 않은 사람을 찾는다.

**정답과 이유**

안 된다. 9월 말 가입자는 후속 30일 관측이 끝나지 않았다. 충분한 관측 기회를 가진 집단만 포함하거나 아직 미성숙한 코호트라고 별도 표시한다.

[해당 강의로 돌아가기](#sql-i08)

<a id="sql-i09-1"></a>
### SQL-I09-1

**문제:** valid_to 조건을 <=로 바꾸면 경계 사건은 몇 행과 연결되는가?

**힌트:** 8월 2일은 양쪽 기간에 들어갈 수 있다.

**정답과 이유**

두 행과 연결되어 fan-out이 생긴다. 이전 버전의 끝은 제외하고 새 버전의 시작은 포함하는 반열린 구간이 이를 방지한다.

[해당 강의로 돌아가기](#sql-i09)

<a id="sql-i09-2"></a>
### SQL-I09-2

**문제:** visits에 client_id와 branch_id가 함께 있는 것은 항상 나쁜 중복인가?

**힌트:** 두 속성의 시간 의미를 확인한다.

**정답과 이유**

항상 그렇지 않다. 방문 당시 서비스 지점이라는 독립적인 사건 속성이라면 필요할 수 있다. 현재 고객 지점 복사본인지 당시 지점 기록인지 데이터 사전에 명확히 정한다.

[해당 강의로 돌아가기](#sql-i09)

<a id="sql-i10-1"></a>
### SQL-I10-1

**문제:** 전체 지연율을 지점 집계에서 다시 계산하는 공식을 쓰고 값을 구하라.

**힌트:** 지점 비율의 평균이 아니라 합계를 나눈다.

**정답과 이유**

(3+3+0+0)/(6+6+3+0)=6/15=40%다. 완료 미측정 합계는 3, 완료 합계는 18이다.

[해당 강의로 돌아가기](#sql-i10)

<a id="sql-i10-2"></a>
### SQL-I10-2

**문제:** 지점2와 지점1 모두 50%이니 상황이 같다고 말할 수 있는가?

**힌트:** 측정 누락을 비교한다.

**정답과 이유**

아니다. 지점2는 완료 9건 중 3건이 미측정이고 지점1은 완료 6건을 모두 측정했다. 관측된 지연율은 같지만 전체 상태에 대한 불확실성은 다르다.

[해당 강의로 돌아가기](#sql-i10)

## Python 심화: 문제 해결 알고리즘과 운영 가능한 서비스

<a id="py-a01-1"></a>
### PY-A01-1

**문제:** 직원 ID 중복을 허용하는 N:N 연결의 색인은 어떤 구조여야 하는가?

**힌트:** 키 하나가 여러 행을 가리켜야 한다.

**정답과 이유**

`dict[str,list[dict]]`처럼 키마다 행 목록을 보관한다. 각 방문에 대해 해당 목록을 모두 순회하므로 O(n+m+k), k는 출력 행 수다. 출력 폭증 자체는 알고리즘으로 없앨 수 없으므로 업무상 필요한 관계인지 확인한다.

[해당 강의로 돌아가기](#py-a01)

<a id="py-a01-2"></a>
### PY-A01-2

**문제:** 방문이 100만 건이고 직원이 1000명이면 단순 중첩 반복의 비교 규모는?

**힌트:** n×m과 n+m을 비교한다.

**정답과 이유**

약 10억 비교다. 색인 방식은 평균적인 모델에서 약 100만+1000 규모의 적재·조회로 바뀐다. 실제 실행 시간을 이 숫자로 단정하지 말고 대표 입력으로 측정한다.

[해당 강의로 돌아가기](#py-a01)

<a id="py-a02-1"></a>
### PY-A02-1

**문제:** payments에서 clients로 되돌아가는 간선을 추가하면 무한 반복하는가?

**힌트:** seen이 이미 방문한 정점을 막는다.

**정답과 이유**

무한 반복하지 않는다. clients는 처음에 seen에 들어 있으므로 다시 큐에 추가되지 않는다. 다만 의존성 그래프에 순환이 존재한다는 별도 진단은 필요할 수 있다.

[해당 강의로 돌아가기](#py-a02)

<a id="py-a02-2"></a>
### PY-A02-2

**문제:** 최단 경로 자체를 돌려주려면 무엇을 추가로 저장해야 하는가?

**힌트:** 각 정점에 처음 도달한 직전 정점을 기록한다.

**정답과 이유**

parent[neighbor]=node를 처음 발견할 때 저장한다. 목표에서 parent를 거꾸로 따라간 뒤 순서를 뒤집는다. 도달 불가능이면 경로 없음으로 반환하고, 가중치 없는 그래프라는 전제를 명시한다.

[해당 강의로 돌아가기](#py-a02)

<a id="py-a03-1"></a>
### PY-A03-1

**문제:** [(0,10),(10,20),(20,30)]의 필요 인력은?

**힌트:** 끝 시각과 시작 시각이 같으면 이전 업무를 제거한다.

**정답과 이유**

1명이다. 이동·준비 시간이 0이라는 가정에서만 그렇다. 방문 사이에 5분 준비 시간이 필요하면 모델에 그 시간을 포함해야 한다.

[해당 강의로 돌아가기](#py-a03)

<a id="py-a03-2"></a>
### PY-A03-2

**문제:** 직원마다 자격이 다르면 위 결과 2명이 충분함을 보장하는가?

**힌트:** 동시 방문 수 외의 제약을 찾는다.

**정답과 이유**

보장하지 않는다. 겹치는 두 방문을 모두 특정 자격자만 수행할 수 있는데 자격자가 한 명이면 불가능하다. 자격·가용 시간·이동 제약을 포함한 매칭 또는 제약 최적화가 필요하다.

[해당 강의로 돌아가기](#py-a03)

<a id="py-a04-1"></a>
### PY-A04-1

**문제:** 세마포어 값을 1로 바꾸면 어떤 점이 달라지는가?

**힌트:** 동시에 대기 구간에 들어가는 작업 수를 본다.

**정답과 이유**

한 번에 하나만 해당 구간에 들어가므로 세 작업이 그 구간을 순차 처리한다. 결과 목록은 같고 완료 시간의 경향은 달라질 수 있다. 수치 성능은 실제 환경에서 측정한다.

[해당 강의로 돌아가기](#py-a04)

<a id="py-a04-2"></a>
### PY-A04-2

**문제:** await은 누가 기다린다는 뜻인지 두 문장으로 설명하라.

**힌트:** 현재 코루틴과 이벤트 루프를 분리해 말한다.

**정답과 이유**

대기가 필요한 await 지점에서는 현재 코루틴의 다음 단계가 잠시 보류된다. 이벤트 루프는 그동안 다른 실행 가능한 Task나 입출력 완료를 처리하며, 준비되면 현재 코루틴을 다시 진행시킨다.

[해당 강의로 돌아가기](#py-a04)

<a id="py-a05-1"></a>
### PY-A05-1

**문제:** 워터마크를 적재 전에 먼저 저장하면 어떤 손실이 생길 수 있는가?

**힌트:** 워터마크 저장 후 배치가 실패했다고 가정한다.

**정답과 이유**

다음 실행이 실패한 범위를 이미 처리된 것으로 보고 건너뛸 수 있다. 적재 성공과 체크포인트 확정을 같은 트랜잭션 또는 일관된 커밋 프로토콜로 묶어야 한다.

[해당 강의로 돌아가기](#py-a05)

<a id="py-a05-2"></a>
### PY-A05-2

**문제:** 중복 이벤트 때문에 상태 행 수가 2인 것은 원본 6행 중 4행을 잃었다는 뜻인가?

**힌트:** 정상 수정본·격리 행·현재 상태를 구분한다.

**정답과 이유**

아니다. 현재 상태는 방문별 최종 한 행이므로 정상 입력 3행이 2개 방문으로 합쳐진다. 잘못된 3행은 격리에 남는다. 다만 이 예제는 정상 이벤트의 전체 감사 이력 표가 없으므로 실제 이력 요구가 있으면 추가해야 한다.

[해당 강의로 돌아가기](#py-a05)

<a id="py-a06-1"></a>
### PY-A06-1

**문제:** limit=101과 허용되지 않은 지점 요청은 각각 어떤 상태 코드가 예상되는가?

**힌트:** 입력 형식 실패와 권한 실패를 분리한다.

**정답과 이유**

limit=101은 이 API의 범위 검증에서 422, 유효한 범위지만 권한 없는 지점은 403이다. 데모 키가 없거나 알려지지 않으면 401이다.

[해당 강의로 돌아가기](#py-a06)

<a id="py-a06-2"></a>
### PY-A06-2

**문제:** after_id 기반 조회가 안정적이기 위한 전제와 한계를 설명하라.

**힌트:** 정렬 키의 불변성과 조회 중 데이터 변경을 생각한다.

**정답과 이유**

visit_id가 고유하고 불변이며 ORDER BY visit_id를 써야 한다. 그러나 여러 페이지 전체를 같은 시점의 스냅샷으로 보장하지는 않는다. 조회 중 삭제·추가·필터 속성 수정이 허용되는지와 스냅샷 요구를 별도로 정한다.

[해당 강의로 돌아가기](#py-a06)

<a id="py-a07-1"></a>
### PY-A07-1

**문제:** DB 없이 지점2 결과를 테스트하는 코드를 작성하라.

**힌트:** 메모리 저장소에 이미 기대값을 넣을 수 있다.

**정답과 이유**

```python
repo = MemoryVisitRepository({2: 9})
assert branch_summary(repo, 2) == {"branch_id": 2, "completed": 9}
```

[해당 강의로 돌아가기](#py-a07)

<a id="py-a07-2"></a>
### PY-A07-2

**문제:** 모든 함수에 인터페이스와 클래스를 만들면 더 좋은 설계인가?

**힌트:** 얻는 변경 격리와 늘어나는 복잡도를 비교한다.

**정답과 이유**

아니다. 안정적이고 단순한 계산은 함수로 충분할 수 있다. 외부 I/O·여러 구현·독립 테스트 필요처럼 구체적인 이유가 있는 경계에서 추상화를 사용한다.

[해당 강의로 돌아가기](#py-a07)

<a id="py-a08-1"></a>
### PY-A08-1

**문제:** 외부 API 대기가 대부분인 경우와 순수 Python 대규모 계산이 대부분인 경우에 먼저 고려할 방법은?

**힌트:** 작업 병목의 종류에 따라 도구가 다르다.

**정답과 이유**

API 대기는 비동기 I/O와 동시성 제한·연결 재사용을, 계산은 알고리즘 개선·벡터화·프로세스 분리 등을 검토한다. 어느 경우든 먼저 측정하고 결과 정확성을 유지한다.

[해당 강의로 돌아가기](#py-a08)

<a id="py-a08-2"></a>
### PY-A08-2

**문제:** 캐시를 넣으면 왜 데이터 정확성 문제가 생길 수 있는가?

**힌트:** 캐시 키와 만료·권한 맥락을 생각한다.

**정답과 이유**

오래된 결과, 다른 지점 결과 재사용, 권한 변경 미반영이 생길 수 있다. 키에 필요한 필터·권한 범위를 포함하고 만료·무효화·원본 신선도 표시를 설계한다.

[해당 강의로 돌아가기](#py-a08)

<a id="py-a09-1"></a>
### PY-A09-1

**문제:** 정상 입력 3행과 현재 상태 2행을 보고 1행 손실이라고 알림을 보내도 되는가?

**힌트:** 이벤트 grain과 상태 grain을 구분한다.

**정답과 이유**

바로 손실이라고 할 수 없다. 같은 방문의 수정 이벤트가 하나의 현재 상태로 합쳐질 수 있다. 기대하는 grain별 건수와 중복 제거 수를 함께 기록해야 한다.

[해당 강의로 돌아가기](#py-a09)

<a id="py-a09-2"></a>
### PY-A09-2

**문제:** 서비스 요청 성공률이 높아도 오래된 데이터를 반환하는 문제를 찾으려면 어떤 지표가 필요한가?

**힌트:** 마지막 처리 시각과 원천 수신 시각을 비교한다.

**정답과 이유**

원천의 마지막 유효 이벤트/수신 시각, 최종 성공 배치 시각, 소비 가능한 마트의 기준 시각, 데이터 신선도 지연을 관찰한다. 단순 HTTP 성공률만으로는 발견하기 어렵다.

[해당 강의로 돌아가기](#py-a09)

<a id="py-a10-1"></a>
### PY-A10-1

**문제:** 각 파티션의 지연율만 저장하면 전체 지연율을 항상 복원할 수 있는가?

**힌트:** 분모 크기가 다를 때를 생각한다.

**정답과 이유**

항상 복원할 수 없다. 같은 50%라도 1/2와 100/200의 비중은 다르다. 최소한 분자·분모를 함께 저장하고, 중복 없는 분할과 지표 버전 일치도 보장한다.

[해당 강의로 돌아가기](#py-a10)

<a id="py-a10-2"></a>
### PY-A10-2

**문제:** 방문 재배정을 단순 DB UPDATE가 아니라 업무 행동으로 설계할 때 필요한 항목은?

**힌트:** 누가·무엇을·언제·어떤 조건에서·실패 시를 묻는다.

**정답과 이유**

호출자 권한, 기존 담당자/버전, 새 직원 자격·시간 충돌, 변경 사유, 승인 필요 여부, 원자적 갱신, 감사 이벤트, 중복 요청 처리, 알림 실패 대응, 되돌리기 정책을 명시한다.

[해당 강의로 돌아가기](#py-a10)

## SQL 심화: 성능·정합성·보안·데이터 시스템 설계

<a id="sql-a01-1"></a>
### SQL-A01-1

**문제:** 추정 10행인데 실제 100만 행이면 무엇부터 의심하는가?

**힌트:** 통계, 분포, 조건 사이 상관관계를 생각한다.

**정답과 이유**

통계의 최신성, 데이터 편향, 조건의 선택도와 상관관계를 확인한다. 잘못된 추정이 JOIN 방식이나 정렬 계획에 영향을 줄 수 있다. 통계 갱신 후에도 문제가 남으면 쿼리 구조와 인덱스, 확장 통계 등 PostgreSQL의 해당 기능을 검토한다.

[해당 강의로 돌아가기](#sql-a01)

<a id="sql-a01-2"></a>
### SQL-A01-2

**문제:** 인덱스 생성 뒤 단 한 번 20ms→10ms가 나오면 개선을 확정할 수 있는가?

**힌트:** 캐시와 결과 동일성을 분리해 본다.

**정답과 이유**

확정할 수 없다. 같은 데이터·결과·부하에서 여러 차례 측정하고 캐시 상태와 실행 계획을 기록한다. 평균뿐 아니라 긴 지연의 분포와 쓰기 비용도 검토한다. 목표는 인덱스 사용 자체가 아니라 업무에 필요한 성능과 정합성이다.

[해당 강의로 돌아가기](#sql-a01)

<a id="sql-a02-1"></a>
### SQL-A02-1

**문제:** 한국 날짜 8월 1일만 조회하려면 UTC 경계는?

**힌트:** 서울은 UTC+9인 이 실습의 시간 계약을 사용한다.

**정답과 이유**

시작은 2026-07-31 15:00:00+00, 끝은 2026-08-01 15:00:00+00이다. `scheduled_start >= 시작 AND scheduled_start < 끝`으로 조회한다. 다른 지역의 시간대는 DST 등 규칙을 따르므로 고정 9시간 빼기를 일반화하지 않는다.

[해당 강의로 돌아가기](#sql-a02)

<a id="sql-a02-2"></a>
### SQL-A02-2

**문제:** planned 조회가 빨라지지 않은 이유를 설명하라.

**힌트:** 부분 인덱스 안에 어떤 행이 있는가?

**정답과 이유**

이 부분 인덱스에는 completed 행만 있다. planned를 위한 조회는 다른 색인이나 순차 스캔을 사용할 수 있다. 상태별 데이터 크기와 사용 빈도를 보고 전체 복합 인덱스 또는 다른 부분 인덱스를 검토한다.

[해당 강의로 돌아가기](#sql-a02)

<a id="sql-a03-1"></a>
### SQL-A03-1

**문제:** 두 번째 UPDATE가 0행인데 API가 성공을 돌려주면 어떤 문제가 생기는가?

**힌트:** 실제 상태와 사용자 믿음이 달라진다.

**정답과 이유**

사용자는 worker_id=3으로 배정되었다고 믿지만 실제로는 2다. 변경 행 수 또는 RETURNING 결과를 검사하고 충돌 응답과 최신 상태 재조회 흐름을 제공한다. 외부에 대상 존재 여부를 공개해도 되는지도 권한 정책에 따라 결정한다.

[해당 강의로 돌아가기](#sql-a03)

<a id="sql-a03-2"></a>
### SQL-A03-2

**문제:** SELECT FOR UPDATE는 낙관적 잠금과 무엇이 다른가?

**힌트:** 먼저 잠그는 방식과 나중에 버전을 확인하는 방식을 비교한다.

**정답과 이유**

트랜잭션에서 행 잠금을 확보해 관련 동시 쓰기를 조정하는 비관적 접근이다. 대기·교착·잠금 유지 시간이 생기므로 트랜잭션을 짧게 유지한다. 외부 HTTP 응답을 기다리는 동안 DB 행 잠금을 오래 잡지 않는다.

[해당 강의로 돌아가기](#sql-a03)

<a id="sql-a04-1"></a>
### SQL-A04-1

**문제:** 감사 INSERT가 실패하면 UPDATE도 취소되어야 하는가?

**힌트:** 감사 없는 변경을 허용하는지 요구사항을 생각한다.

**정답과 이유**

이 계약에서는 함께 취소되어야 한다. 동일 명령과 트랜잭션에 묶어 실패 시 전부 롤백한다. 오류를 잡고 UPDATE만 커밋하는 코드는 계약을 깨뜨린다.

[해당 강의로 돌아가기](#sql-a04)

<a id="sql-a04-2"></a>
### SQL-A04-2

**문제:** 완료 처리 후 이메일을 보내다가 실패했다면 UPDATE를 다시 하면 되는가?

**힌트:** DB 상태와 이메일은 서로 다른 자원이다.

**정답과 이유**

그대로 재실행하면 중복 이메일 또는 누락이 생길 수 있다. 완료 상태 변경과 outbox 이벤트 저장을 같은 트랜잭션으로 처리하고, 별도 소비자가 안정된 이벤트 ID로 중복을 억제하며 전송·재시도 결과를 기록하는 설계를 검토한다.

[해당 강의로 돌아가기](#sql-a04)

<a id="sql-a05-1"></a>
### SQL-A05-1

**문제:** 워터마크를 100으로 저장한 뒤 91~100 반영 중 실패하면?

**힌트:** 다음 실행이 어디서 시작하는가?

**정답과 이유**

다음 실행이 101부터 시작하면 91~100의 일부가 영구 누락된다. 반영 성공과 진행 위치 저장을 원자적으로 처리하거나 재처리 가능한 범위를 다시 읽고 타깃을 멱등하게 갱신해야 한다.

[해당 강의로 돌아가기](#sql-a05)

<a id="sql-a05-2"></a>
### SQL-A05-2

**문제:** updated_at만 같은 두 이벤트의 순서를 어떻게 정하는가?

**힌트:** 업무 의미가 있는 순서와 임의 순서를 구분한다.

**정답과 이유**

원천 sequence/version이 있으면 그것을 사용한다. 없으면 타이브레이커를 합의하되 임의 event_id 정렬이 실제 업무 최신성을 보장한다고 주장하지 않는다. 충돌을 격리해 해결하는 정책도 가능하다.

[해당 강의로 돌아가기](#sql-a05)

<a id="sql-a06-1"></a>
### SQL-A06-1

**문제:** cutoff를 8월 4일로 바꾸면 어떤 이벤트가 선택되는가?

**힌트:** 두 시간 조건을 모두 만족한 후보 중 최신 사건을 고른다.

**정답과 이유**

event_id=2, paused가 선택된다. 이제 available_at도 cutoff 이전이다. 다만 미래 유효 이벤트나 정정 우선순위가 있다면 별도 모델 규칙을 추가해야 한다.

[해당 강의로 돌아가기](#sql-a06)

<a id="sql-a06-2"></a>
### SQL-A06-2

**문제:** 현재 상태 하나만 저장한 표로 8월 2일의 판단을 완벽히 재현할 수 있는가?

**힌트:** 과거 정보와 변경 시점을 보관했는가?

**정답과 이유**

일반적으로 보장할 수 없다. append-only 이벤트나 변경 이력, 유효 시점과 기록 시점이 필요하다. 이미 없어진 과거를 근거 없이 복원하지 말고 재현 가능한 범위를 명시한다.

[해당 강의로 돌아가기](#sql-a06)

<a id="sql-a07-1"></a>
### SQL-A07-1

**문제:** UNION을 UNION ALL로 바꾸면 무엇이 달라지는가?

**힌트:** clients가 다시 발견되었을 때 제거되는가?

**정답과 이유**

중복 정점이 제거되지 않아 순환을 계속 따라갈 수 있다. 실제 실행 전에 순환 방지 로직을 구현한다. 단순히 LIMIT으로 화면에 몇 행만 보이는 것을 정확한 종료 정책으로 삼지 않는다.

[해당 강의로 돌아가기](#sql-a07)

<a id="sql-a07-2"></a>
### SQL-A07-2

**문제:** clients에서 workers까지의 영향이 필요하면 간선을 거꾸로 추가하면 되는가?

**힌트:** 이 관계가 실제 업무 의존성인지 따져 본다.

**정답과 이유**

기계적으로 추가하기 전에 영향의 의미를 정한다. 출처 추적이면 그래프 방향을 반대로 탐색하는 것이 적절할 수 있다. 하류 영향 분석과 상류 원인 추적을 같은 화면에서 구분해야 한다.

[해당 강의로 돌아가기](#sql-a07)

<a id="sql-a08-1"></a>
### SQL-A08-1

**문제:** WHERE branch_id=사용자입력만 있으면 지점 격리가 되는가?

**힌트:** 그 사용자가 그 지점에 접근해도 되는지 확인했는가?

**정답과 이유**

아니다. 인증된 주체의 허용 지점과 요청 지점을 비교해야 한다. 서버 권한 검사와 적절한 DB 권한/RLS를 다층적으로 적용한다. 사용자가 요청값을 바꾸는 테스트를 반드시 수행한다.

[해당 강의로 돌아가기](#sql-a08)

<a id="sql-a08-2"></a>
### SQL-A08-2

**문제:** 관리자 계정의 RLS 테스트만 통과하면 충분한가?

**힌트:** 실제 앱 역할과 예외 적용 여부가 다른가?

**정답과 이유**

충분하지 않다. 수퍼유저·BYPASSRLS·소유자 적용 예외를 확인하고 실제 앱 역할로 테스트한다. 관리자 연결을 그대로 웹 요청 처리에 사용하지 않는다.

[해당 강의로 돌아가기](#sql-a08)

<a id="sql-a09-1"></a>
### SQL-A09-1

**문제:** 2026-09-01 행을 넣으면 어디에 들어가는가?

**힌트:** [8월1일,9월1일) 경계를 확인한다.

**정답과 이유**

현재는 대응 파티션이 없어 INSERT가 실패한다. 8월 파티션에 들어가지 않는다. 9월 파티션 생성 또는 명시적으로 합의한 기본/격리 정책이 필요하다.

[해당 강의로 돌아가기](#sql-a09)

<a id="sql-a09-2"></a>
### SQL-A09-2

**문제:** PRIMARY KEY(event_id,event_day)는 event_id 단독 고유성을 보장하는가?

**힌트:** 같은 ID와 다른 날짜 조합을 생각한다.

**정답과 이유**

보장하지 않는다. 날짜가 다르면 같은 event_id가 들어갈 수 있다. 원천 ID의 의미와 전역 고유성 요구를 확인하고 별도 키 레지스트리, 다른 파티션 키, 또는 비파티션 설계를 검토한다.

[해당 강의로 돌아가기](#sql-a09)

<a id="sql-a10-1"></a>
### SQL-A10-1

**문제:** 결측 3건을 0분으로 채우면 지연율은 어떻게 달라지는가?

**힌트:** 분자와 분모가 바뀌는지 본다.

**정답과 이유**

6/18≈33.33%가 된다. 이는 미확인 방문을 정시로 가정한 별도의 지표다. 원래 확정 가능한 완료 15건 기준 40%와 섞어 보고하면 안 된다. 최선/최악 가정 범위 6/18~9/18도 신뢰구간이 아니라 결측에 대한 가정 범위다.

[해당 강의로 돌아가기](#sql-a10)

<a id="sql-a10-2"></a>
### SQL-A10-2

**문제:** 집계 표는 빠르지만 마지막 갱신이 어제다. 어떻게 표시하고 운영하는가?

**힌트:** 성능 외에 데이터 기준 시각과 의사결정 적합성을 생각한다.

**정답과 이유**

화면에 기준 시각·원천 최신 시각·갱신 성공 여부를 표시하고 허용 지연을 넘으면 경고 또는 사용 차단한다. 새로고침 실패 알림·재처리·소유자를 지정한다. 사용자가 수동 새로고침을 눌렀다는 사실만으로 데이터가 최신이 된다고 주장하지 않는다.

[해당 강의로 돌아가기](#sql-a10)



---
